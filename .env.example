# Galaxy Miner Environment Configuration
# Copy this file to .env and customize values for your environment

# ====================
# Server Configuration
# ====================

# Server port (default: 3388)
PORT=3388

# Server host/bind address (default: 0.0.0.0)
HOST=0.0.0.0

# Environment mode: development, production, test
NODE_ENV=development

# ====================
# Security (REQUIRED FOR PRODUCTION)
# ====================

# Session secret for token signing
# MUST be set in production - at least 32 characters
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=your-secure-secret-at-least-32-characters-long

# ====================
# Authentication
# ====================

# Token expiry time in milliseconds (default: 86400000 = 24 hours)
TOKEN_EXPIRY_MS=86400000

# Login rate limit - max attempts per minute per IP (default: 5)
LOGIN_RATE_LIMIT=5

# Registration rate limit - max attempts per minute per IP (default: 3)
REGISTER_RATE_LIMIT=3

# ====================
# Game Timing
# ====================

# Position save interval in milliseconds (default: 5000 = 5 seconds)
POSITION_SAVE_INTERVAL_MS=5000

# Wreckage despawn time in milliseconds (default: 120000 = 2 minutes)
WRECKAGE_DESPAWN_TIME_MS=120000

# ====================
# Wormhole System
# ====================

# Wormhole transit duration in milliseconds (default: 5000 = 5 seconds)
TRANSIT_DURATION_MS=5000

# Wormhole entry range in units (default: 100)
WORMHOLE_RANGE=100

# Destination selection timeout in milliseconds (default: 30000 = 30 seconds)
SELECTION_TIMEOUT_MS=30000

# ====================
# Spawning
# ====================

# Safe spawn radius search distance (default: 5000)
SAFE_SPAWN_RADIUS=5000

# ====================
# Debug
# ====================

# Enable debug logging (default: true in development, false in production)
# When DEBUG=false (production):
#   - logger.log/info/warn are silenced
#   - logger.category() is silenced
#   - logger.error() always logs (errors and exceptions)
#   - logger.network() always logs (connection/auth events)
# DEBUG=true
